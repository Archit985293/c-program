#include <stdio.h>

int main() {
    int n = 5; // number of rows in the top half (including middle row)

    // Top half (including middle)
    for (int i = 1; i <= n; i++) {
        int stars = 2 * i - 1; // odd numbers: 1,3,5...
        int spaces = n - i;

        // print leading spaces
        for (int s = 1; s <= spaces; s++) {
            printf(" ");
        }

        // print stars
        for (int j = 1; j <= stars; j++) {
            printf("*");
        }

        printf("\n");
    }

    // Bottom half
    for (int i = n - 1; i >= 1; i--) {
        int stars = 2 * i - 1;
        int spaces = n - i;

        // print leading spaces
        for (int s = 1; s <= spaces; s++) {
            printf(" ");
        }

        // print stars
        for (int j = 1; j <= stars; j++) {
            printf("*");
        }

        printf("\n");
    }

    return 0;
}

#include <stdio.h>

int main() {
    int n, count = 1;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    for (count = 1; count <= n; count++) {
        printf("%d ", count);
    }

    return 0;
}
